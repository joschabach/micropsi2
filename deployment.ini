[app:cortex]
use = egg:psi_cortex
admin_api_host = 0.0.0.0
admin_api_port = 8008
data_path = /mnt/data/cortex

[server:main]
use = egg:waitress#main
host = 0.0.0.0
port = 8009

[filter:translogger]
use = egg:Paste#translogger

[pipeline:main]
pipeline = translogger
           cortex
