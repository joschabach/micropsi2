[buildout]
extends = app.cfg
parts +=
    pytest

[config]
app_user =
inifile = development.ini

[pytest]
recipe = z3c.recipe.scripts
scripts = py.test=test
eggs =
    psi_cortex [tests]
    pytest
arguments = ['micropsi_server/tests', 'micropsi_core/tests'] + sys.argv[1:]

